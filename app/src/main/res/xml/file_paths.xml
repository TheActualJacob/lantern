<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Zipped sessions are written here before being shared via FileProvider. -->
    <cache-path
        name="shared"
        path="shared/" />
</paths>
